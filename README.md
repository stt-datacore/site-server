# Dynamic bits of datacore.app
TODO: write-up

a `static` folder in the repo's root will serve as the location for all uploaded player profiles
a `logs` folder in the repo's root will hold error and warning logs